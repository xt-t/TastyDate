export interface RestaurantCard {
    id: string,
    cardCreator: string;
    restaurantName: string,
    category: string,
    rating: number,
    price: number
    postcode: number,
    city: string,
}